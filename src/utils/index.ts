import {
  createElement,
  createIdent,
} from './helper'

import {
  toDate,
  dateFormat,
  createTimeList
} from './dateHelper'



const utils: IUtils = {
  createElement,
  createIdent,
  toDate,
  dateFormat,
  createTimeList
}

export default utils
