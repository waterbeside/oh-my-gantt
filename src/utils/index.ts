import {
  createElement,
  createIdent,
} from './helper'

import {
  toDate,
  dateFormat,
  createTimeList
} from './dateHelper'

import { IUtils } from '../../typings/types'


const utils: IUtils = {
  createElement,
  createIdent,
  toDate,
  dateFormat,
  createTimeList
}

export default utils
